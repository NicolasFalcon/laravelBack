<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
    <style>
        body {
            font-family: Arial, Helvetica, sans-serif;
            background-color: #f2f2f2;
            text-align: center;
        }

        .login-container {
            background-color: #fff;
            border-radius: 5px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.2);
            width: 300px;
            margin: 0 auto;
            margin-top: 100px;
            padding: 56px;
        }

        h2 {
            color: #333;
        }

        .input-group {
            margin: 10px 0;
        }

        .input-group label {
            display: block;
            text-align: left;
            margin: 3px;
        }

        .input-group input {
            width: 100%;
            padding: 10px;
            margin: 5px 0;
            border: 1px solid #ccc;
            border-radius: 3px;
        }

        .input-group button {
            width: 100%;
            padding: 10px;
            background-color: #333;
            color: #fff;
            border: none;
            border-radius: 3px;
            cursor: pointer;
        }

        .input-group button:hover {
            background-color: #555;
        }
        .alert.alert-danger {
            color: #ff0000; /* Red color */
            background-color: #f8d7da; /* Background color for error alerts (optional) */
            border: 1px solid #f5c6cb; /* Border color (optional) */
            padding: 10px; /* Adjust as needed */
            border-radius: 5px; /* Rounded corners (optional) */
        }
    </style>
</head>
<body>
    <div class="login-container">
        <h2>Login</h2>
    </div>
</body>
</html>